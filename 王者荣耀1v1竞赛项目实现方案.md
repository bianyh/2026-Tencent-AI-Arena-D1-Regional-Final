# 王者荣耀 1v1 竞赛项目实现方案（基于当前代码仓）

更新时间：2026-05-09  
适用代码目录：`./agent_diy`（自定义实现），参考基线：`./agent_ppo`

---

## 1. 项目现状分析

### 1.1 代码结构结论

当前仓库已经具备两套入口：

- `agent_ppo`：官方完整可运行 PPO baseline（含特征、奖励、模型、算法、工作流）。
- `agent_diy`：官方预留模板，核心逻辑基本空实现，需要我们完整补齐。

关键文件状态如下：

1. `agent_diy/agent.py`：`reset`、`_model_inference`、`observation_process`、`action_process` 未实现。
2. `agent_diy/model/model.py`：网络结构未实现。
3. `agent_diy/algorithm/algorithm.py`：`learn` 未实现。
4. `agent_diy/feature/definition.py`：`FrameCollector.sample_process` 未实现，训练样本管线不完整。
5. `agent_diy/workflow/train_workflow.py`：训练主循环未实现。
6. `agent_diy/conf/conf.py`：核心超参数为空。

结论：`agent_diy` 当前不可训练、不可评估、不可提交。

### 1.2 竞赛环境约束（对实现影响最大）

1. 1v1，英雄仅 `112`（鲁班七号）和 `133`（狄仁杰）。
2. 动作空间固定：`[12, 16, 16, 16, 16, 9]`，并包含 legal action + sub-action mask 约束。
3. 训练流程以 `env.reset / env.step` 的 dict 协议交互，评估调用 `agent.exploit`。
4. 终局由推塔、超时（20000 帧）、异常决定；训练 reward 需自行设计（非直接比赛得分）。

### 1.3 可直接复用资产

`agent_ppo` 中以下实现具备直接迁移价值：

- 合法动作掩码采样逻辑（含 target 分支 mask）。
- GAE 样本构造 + PPO loss。
- 自对弈工作流（含 common_ai / selfplay / 指定模型对手）。
- 召唤师技能初始化接口。
- 最小可用特征与奖励闭环（10 维特征 + 简单奖励）。

---

## 2. 总体目标与策略

## 2.1 总体目标

在 `agent_diy` 中构建一套**可训练、可评估、可持续迭代提分**的 1v1 智能体方案，并具备以下能力：

1. 稳定跑通训练与评估流程。
2. 指标可观测（reward/loss/win_rate）。
3. 支持分阶段优化（特征、奖励、对手池、超参）。

## 2.2 实施策略

采用“两阶段主线 + 多轮提分迭代”的保守方案：

1. **阶段 A（可运行）**：先把 `agent_ppo` 能力等价迁移到 `agent_diy`，确保可训练。
2. **阶段 B（可提分）**：在不破坏稳定性的前提下，逐项增强特征、奖励和对战策略。

这样做的好处是：先拿到可验证基线，再做受控优化，避免一上来大改导致无法定位问题。

---

## 3. 技术方案总览

### 3.1 算法选择

首选 PPO（on-policy），理由：

1. 官方 baseline 已验证链路。
2. 动作 mask、多分支离散动作适配成熟。
3. 在 1v1 对抗任务上训练稳定性优于从零设计新算法。

### 3.2 系统架构（DIY 侧）

1. `workflow/train_workflow.py`：环境循环、对手配置、采样与样本发送。
2. `agent.py`：观测处理、推理、动作构造、模型加载保存。
3. `feature/definition.py`：`ObsData/ActData/SampleData`、帧收集、GAE、样本打包。
4. `model/model.py`：策略网络 + 价值网络。
5. `algorithm/algorithm.py`：PPO 损失与反向更新。
6. `conf/conf.py`：维度定义、超参定义、训练常量。
7. `conf/monitor_builder.py`：训练监控面板。

---

## 4. 分阶段实现计划（详细）

## 4.1 阶段 A：先跑通（Baseline 迁移）

目标：`agent_diy` 行为与 `agent_ppo` 在结构上等价，训练可启动，`train_test.py` 通过。

### A1. 配置层落地（`agent_diy/conf/conf.py`）

实现内容：

1. 增加 `GameConfig`（奖励权重、模型保存间隔）。
2. 增加 `DimConfig`（初版特征维度）。
3. 增加 `Config`：
   - `LABEL_SIZE_LIST=[12,16,16,16,16,9]`
   - `LEGAL_ACTION_SIZE_LIST`（最后一维乘以 12）
   - `LSTM_TIME_STEPS/LSTM_UNIT_SIZE`
   - `GAMMA/LAMDA`
   - PPO 参数（`CLIP_PARAM`、`BETA_START`、`INIT_LEARNING_RATE_START` 等）
   - `data_shapes` 与 `DATA_SPLIT_SHAPE`

验收标准：

1. 配置可被 `agent.py / model.py / algorithm.py` 全部引用。
2. `SAMPLE_DIM` 计算正确，无维度错位。

### A2. 特征与样本管线落地（`agent_diy/feature/definition.py`）

实现内容：

1. 补全 `build_frame`、`_update_legal_action`、`FrameCollector`。
2. 支持：
   - 帧级缓存（按 frame_no）
   - GAE 计算（advantage, reward_sum）
   - LSTM 序列拼接
   - `SampleData(sample=np.ndarray)` 输出
3. 保持与 `Algorithm.learn` 输入切分对齐。

验收标准：

1. 能持续产出非空 sample。
2. `sample.shape` 与 `Config.SAMPLE_DIM` 一致。

### A3. 模型落地（`agent_diy/model/model.py`）

实现内容：

1. 复用官方 PPO 的“共享干线 + 多头策略 + 价值头”结构。
2. 支持 `forward(inference=True/False)` 两种模式。
3. 提供 `compute_loss`：
   - masked policy loss（PPO clip）
   - value loss
   - entropy loss

验收标准：

1. 前向可跑，输出 logits/value/lstm state。
2. loss 可反向传播，无 NaN。

### A4. 算法落地（`agent_diy/algorithm/algorithm.py`）

实现内容：

1. 完整实现 `learn(list_sample_data)`：
   - stack 样本
   - 按 cut points 切分
   - 调模型前向 + loss
   - backward + grad clip + optimizer/scheduler step
2. 监控上报：
   - `total_loss/value_loss/policy_loss/entropy_loss`

验收标准：

1. 每次 learn 都能正常更新参数。
2. loss 曲线可观测。

### A5. Agent 主逻辑落地（`agent_diy/agent.py`）

实现内容：

1. 完成 `reset`（camp/player/lstm/reward_manager/feature_process 初始化）。
2. 完成 `observation_process`（特征+legal_action 打包 ObsData）。
3. 完成 `_model_inference`：
   - tensor 化输入
   - 模型推理
   - 合法动作采样
4. 完成 `action_process`（训练随机采样，评估最大概率）。
5. 完成 `update_status`（obs/act/lstm 状态回写）。

验收标准：

1. `predict/exploit` 均返回合法 action。
2. 与环境交互不中断。

### A6. 训练工作流落地（`agent_diy/workflow/train_workflow.py`）

实现内容：

1. 对齐 `agent_ppo` 的 `EpisodeRunner` 流程：
   - 更新对局配置
   - 调 `init_config` 注入召唤师技能
   - reset agents
   - 循环 predict/exploit + env.step
   - reward 计算 + 采样 + send_sample_data
2. 兼容三类对手：
   - `selfplay`
   - `common_ai`
   - 自定义模型 id
3. 训练中定时保存模型。

验收标准：

1. `train_test.py` 设为 `algorithm_name="diy"` 可通过。
2. 连续训练 30 分钟无崩溃。

---

## 4.2 阶段 B：提分增强（在可跑基础上）

## B1. 特征增强（建议从 10 维提升到 40~120 维）

初版 10 维特征过于稀疏，信息不够。建议分三批增加：

1. 英雄核心状态（自/敌）
   - `hp/max_hp`, `ep/max_ep`
   - `level`, `exp`, `money`
   - `mov_spd`, `atk_spd`, `phy_atk`, `phy_def`
2. 战斗态势
   - 敌我距离、敌塔距离、朝向夹角
   - 最近敌方小兵距离/血量
   - 是否在塔攻击范围风险区
3. 技能与节奏
   - 技能可用状态、冷却归一化
   - 召唤师技能可用状态
   - 对手关键技能 CD 估计（从可见帧推断）

实现建议：

1. 新建 `agent_diy/feature/feature_process/`（可直接参考 `agent_ppo` 结构）。
2. 用配置文件管理归一化规则（`*.ini`），便于做实验版本控制。

## B2. 奖励增强（从“推塔+前进”扩展为稳定零和设计）

建议 reward 子项：

1. `tower_hp_delta`（高权重，零和）
2. `hero_hp_delta`（中权重，零和）
3. `kill/death`（稀疏强信号）
4. `money_delta`、`exp_delta`（中低权重）
5. `last_hit`（促进补刀）
6. `unsafe_forward_penalty`（低血深压塔惩罚）
7. `idle_penalty`（抑制原地无效动作）

推荐总公式：

`R = Σ(w_i * r_i)`，其中核心项（塔血、击杀）权重大，行为塑形项（前进、补刀）权重小。

注意：

1. 奖励项必须可解释，可定位。
2. 每次只改 1~2 个奖励权重，避免多变量耦合。

## B3. 召唤师技能与英雄对位策略

在 `init_config` 中加入策略，而不是随机：

1. 鲁班（112）对狄仁杰（133）：优先 `闪现(80115)` 或 `疾跑(80109)`，提高生存与拉扯。
2. 狄仁杰对鲁班：优先 `终结(80108)` 或 `净化(80107)`，提高斩杀与反控能力。
3. 训练早期保留 10% 随机技能探索，后期收敛到策略技能池。

## B4. 对手池与课程学习（Curriculum）

建议训练顺序：

1. 第 0~N 局：`common_ai`，快速建立基本推进能力。
2. 中期：`selfplay(latest vs latest/random)` 混合。
3. 后期：固定强 checkpoint 对手 + latest 对手混训，提升泛化。

推荐配比（示例）：

1. 20% `common_ai`
2. 50% `selfplay latest`
3. 30% 历史强模型池随机抽样

## B5. 超参数调优策略

优先调以下参数：

1. 学习率：`1e-3 -> 5e-4 -> 1e-4` 分段衰减。
2. 熵系数：早期偏高促进探索，后期降低促进收敛。
3. PPO clip：`0.2` 起步，必要时试 `0.1/0.15`。
4. `train_batch_size`：在资源允许下逐步增大，提升梯度稳定性。
5. `replay_buffer_capacity` 与采样/插入比：防止样本过旧或过拟合。

---

## 5. 评估与实验设计

## 5.1 评估指标

主指标：

1. `win_rate`（对 common_ai、对 selfplay、对历史 checkpoint）
2. 推塔效率（`enemy_tower_hp` 下降速度）
3. 对局时长（frame，避免只会拖时长）

辅指标：

1. `money_per_frame`
2. `kill/death`
3. `hurt_to_hero / hurt_by_hero`

## 5.2 实验矩阵（建议）

每轮只改一类变量，记录 3~5 组实验：

1. Reward Ablation：逐项移除奖励，看 win_rate 变化。
2. Feature Ablation：基础特征 vs 增强特征。
3. Opponent Ablation：只 selfplay vs 混合对手池。
4. Summoner 策略：随机 vs 规则策略。

## 5.3 模型管理策略

1. 按平台限制控制保存频率（≤2 次/分钟）。
2. 每日标记“最佳胜率模型”并写入 `kaiwu.json` 作为对手池候选。
3. 记录 checkpoint 元信息：训练步数、对手配置、核心超参、reward 版本号。

---

## 6. 交付物与时间计划（建议）

## 6.1 交付物清单

1. `agent_diy` 可运行训练代码（完整 PPO 链路）。
2. 版本化特征与奖励配置（含说明文档）。
3. 监控面板扩展（loss + reward + 自定义训练指标）。
4. 实验记录文档（每轮改动、指标对比、结论）。

## 6.2 时间排期（可按 2 周节奏）

1. 第 1~2 天：阶段 A（跑通）。
2. 第 3~5 天：特征增强 + 奖励 v2。
3. 第 6~8 天：对手池与召唤师策略。
4. 第 9~11 天：超参与稳定性调优。
5. 第 12~14 天：收敛版本固化 + 报告整理。

---

## 7. 风险与规避

1. 风险：训练跑不起来（样本维度错位）。
   - 规避：先做 A 阶段等价迁移，不混入增强逻辑。
2. 风险：reward 发散导致策略崩溃。
   - 规避：奖励分版本，单变量变更，保留回退 checkpoint。
3. 风险：自对弈过拟合单一策略。
   - 规避：引入历史模型池 + common_ai 混合。
4. 风险：评估波动大，结论不稳定。
   - 规避：固定评估对手与局数，做滑动窗口统计。

---

## 8. 直接执行清单（按文件）

第一轮改造建议严格按以下顺序进行：

1. `agent_diy/conf/conf.py`：补齐所有 Config。
2. `agent_diy/feature/definition.py`：补齐采样链路。
3. `agent_diy/model/model.py`：补齐网络和 loss。
4. `agent_diy/algorithm/algorithm.py`：补齐 learn。
5. `agent_diy/agent.py`：补齐推理与动作选择。
6. `agent_diy/workflow/train_workflow.py`：补齐训练流程。
7. `train_test.py`：切换 `algorithm_name="diy"` 做冒烟验证。

完成以上后，再进入特征/奖励/对手池优化。

---

## 9. 最终验收标准

达到以下条件可视为“具备竞赛迭代能力”：

1. `diy` 可稳定训练并定期产出模型。
2. 监控指标完整（reward + 各 loss + env 指标）。
3. 对 `common_ai` 胜率明显高于随机策略。
4. 自对弈训练中 win_rate 与推塔指标具备可解释上升趋势。
5. 形成至少一版“可复现实验配置”（代码 + 配置 +记录）。

---

如果后续你希望，我可以按这个方案直接进入“阶段 A 代码落地”，先把 `agent_diy` 改到可跑并通过 `train_test`。
