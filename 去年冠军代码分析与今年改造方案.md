# 去年冠军代码分析与今年赛题改造方案

更新时间：2026-05-11  
分析对象：

- 去年冠军代码：`agent_lastyear/`、`conf_lastyear/`
- 今年官方示例：`agent_ppo/`、`conf/`
- 当前状态：已删除此前基于今年 `agent_diy` 的增强 PPO 实现与旧方案文档，准备重新以去年冠军代码为核心设计今年参赛实现。

---

## 1. 当前清理结果

已按要求清理：

1. 删除此前的实现方案文档：`王者荣耀1v1竞赛项目实现方案.md`
2. 删除此前实现的 `agent_diy/` 代码目录

保留：

1. `agent_ppo/`：今年官方 PPO baseline，用于今年协议、工作流和接口参考
2. `agent_lastyear/`：去年冠军代码，用于迁移核心设计
3. `conf/`：今年配置
4. `conf_lastyear/`：去年配置
5. `王者荣耀1v1官方文档整理.md`：今年官方文档整理版

---

## 2. 去年冠军代码总体设计

去年代码不是简单 PPO baseline，而是一套完整的“结构化状态建模 + PPO + LSTM + 实体 target 注意力”的方案。它的核心优势在于：

1. 对原始环境状态做了很细的结构化解包。
2. 通过固定槽位组织英雄、小兵、防御塔、河蟹、血包、子弹。
3. 对位置、血量、技能、buff、金币变化等做了离散化或 one-hot 编码。
4. 模型按实体类型分模块处理，而不是直接把所有数值拼成一个 MLP 输入。
5. target 动作头用实体 embedding 做注意力输出，与动作协议中的 9 类 target 强耦合。
6. 奖励设计包含血量、塔血、经济、经验、能量、击杀死亡、补刀、前进，并加入时间衰减。

关键模块如下：

| 模块 | 作用 |
| --- | --- |
| `agent_lastyear/agent.py` | Agent 主类，负责 reset、predict、exploit、learn、模型加载保存、动作 mask 采样 |
| `feature/unpack_state_dict.py` | 将原始 observation 解包为 `Info/HeroInfo/ActorInfo/SoldierInfo/...` |
| `feature/obs_builder.py` | 将 `Info` 转为高维结构化向量 |
| `feature/reward_manager.py` | 奖励计算 |
| `feature/definition.py` | 样本结构、GAE、LSTM 序列样本打包 |
| `model/model.py` | 实体模块化网络、LSTM、target 注意力、多英雄输出头 |
| `algorithm/algorithm.py` | PPO 训练逻辑 |
| `workflow/train_workflow.py` | 训练流程、自对弈、评估、样本发送 |
| `workflow/env_conf_manager.py` | 训练/评估配置切换，支持多个评估对手随机轮换 |
| `workflow/lineup_heros.py` | 英雄阵容轮换 |

---

## 3. 去年特征工程设计

### 3.1 状态解包层

去年代码先通过 `Info` 类把原始 dict 解包为对象化结构，避免在特征构造里大量散乱访问原始字段。

核心结构：

- `Info`
- `HeroInfo`
- `ActorInfo`
- `SkillInfo`
- `SlotInfo`
- `OrganInfo`
- `SoldierInfo`
- `CakeInfo`
- `BulletsInfo`
- `DeadsInfo`

这个设计非常值得迁移。它的价值是把今年协议差异集中在一层适配，而不是让模型、奖励、工作流全部直接依赖原始 dict。

### 3.2 坐标镜像

去年 `Info.update()` 中根据 `player_camp` 判断是否翻转坐标：

```python
inverse_position = (self.player_camp == 1 and self.reverse_camp1)
```

红方视角坐标会镜像，使模型始终以同一方向理解“推进”和“后撤”。这是 1v1 对称地图中非常关键的设计，今年仍然适用。

今年需要注意：今年 `camp` 字段可能是整数或字符串，官方 baseline 中 `observation["camp"]` 可能为 `"PLAYERCAMP_1"` / `"PLAYERCAMP_2"`，协议文档中也有 `camp=1/2` 的形式。因此今年要写兼容型 camp 转换函数。

### 3.3 单位特征

去年把单位共性特征抽成 `process_unit()`：

- 相对位置 one-hot
- 全局位置 one-hot + 局部比例
- 血量比例 + 离散血量桶
- buff mark 层数

这比我们之前做的少量连续数值特征更稳，原因是：

1. PPO 对离散 one-hot 态势更容易学习规则边界。
2. 小地图 1v1 中位置区间很重要，离散位置能表达“塔前”“兵线附近”等区域。
3. buff/mark 对射手英雄很关键，尤其是带层数的技能机制。

### 3.4 英雄特征

去年英雄特征包括：

- 英雄 ID 编码
- 行为状态：死亡、移动、普攻、使用技能等
- 法力/能量
- 技能 CD 与可用状态
- 等级
- 金币变化与总金币
- 是否在草丛
- 是否在塔攻击范围内
- 是否为塔攻击目标
- buff skill one-hot
- 通用单位特征

今年应保留这套思想，但字段名和英雄 buff ID 必须重建。

去年英雄是：

- `169` 后羿
- `173` 李元芳
- `174` 虞姬

今年英雄是：

- `112` 鲁班七号
- `133` 狄仁杰

因此以下配置必须替换：

- `HERO_CONFIG_ID`
- `BUFFS`
- `MARK_ID_LAYERS`
- 技能槽含义
- 英雄专属 buff/mark 规则

### 3.5 小兵、防御塔、血包、河蟹、子弹

去年特征覆盖：

- 己方最多 4 个小兵
- 敌方最多 4 个小兵
- 河蟹
- 己方防御塔
- 敌方防御塔
- 敌方英雄子弹最多 9 个
- 防御塔子弹 1 个
- 塔后血包是否存在和下次刷新时间

今年官方文档说 target 仍包含 4 个小兵、1 个野怪，因此“小兵 4 槽 + 野怪/河蟹 1 槽”的建模思路仍然合适。但今年官方项目简介没有强调河蟹/血包，文档中 `cakes` 和 `monster` 仍保留协议，是否实际出现需要通过今年平台帧数据确认。

迁移建议：

1. 先实现英雄、小兵、防御塔、子弹。
2. 河蟹与血包保留代码结构，但在今年环境未出现时自动填 0。
3. 不要假设去年 `config_id=6827` 一定还是河蟹，要通过今年调试帧确认。

---

## 4. 去年奖励设计

去年 `GameConfig.REWARD_WEIGHT_DICT`：

```python
{
    "hp_point": 2.0,
    "tower_hp_point": 10.0,
    "money": 4e-3,
    "exp": 4e-3,
    "ep_rate": 0.75,
    "death": -1.0,
    "kill": -0.6,
    "last_hit": 0.5,
    "forward": 0.01,
}
```

说明：

1. `tower_hp_point` 权重最高，明确服务于赛题胜负条件：推掉防御塔。
2. `hp_point` 使用四次方根形式平滑血量变化。
3. `money`、`exp` 使用差分，鼓励补兵和发育。
4. `ep_rate` 鼓励资源健康。
5. `last_hit` 明确鼓励补刀。
6. `forward` 只在早期有效，`REMOVE_FORWARD_AFTER=1000` 后移除，避免中后期无脑前压。
7. `TIME_SCALE_ARG=8000`，让塑形奖励随时间衰减，避免后期 reward shaping 干扰真实目标。

今年应保留：

- 塔血高权重
- 英雄血量差分
- 金钱/经验差分
- 补刀
- 前进早期启发
- 时间衰减

今年要调整：

1. `death` 和 `kill` 的符号要重新审查。去年配置中 `kill=-0.6`，但代码里 kill/death 都会做双方差分；该符号可能是去年实测后的工程调参，不宜直接照搬。
2. 今年鲁班和狄仁杰都是射手，击杀收益可能不如推塔直接，应避免 kill reward 过高导致追人不推塔。
3. 今年可选召唤师技能更多，治疗/闪现/净化/终结带来的生存和斩杀变化要在奖励里体现为血量/塔血，而不是单独奖励技能释放。

---

## 5. 去年模型设计

### 5.1 实体模块化编码

去年模型不是一个简单 MLP，而是按实体类型拆模块：

- 位置共享 MLP：`position_mlp`
- 单位非位置特征共享 MLP：`unit_no_pos_mlp`
- 英雄 MLP
- 小兵 MLP
- 河蟹 MLP
- 防御塔 MLP
- 子弹 MLP

然后将各模块输出拼接，进入：

- `concat_mlp`
- LSTM
- 旁路 MLP
- `lstm_and_linear_mlp`
- policy heads
- value head

这个结构更适合 1v1，因为不同实体的语义不同：

- 英雄是博弈对象
- 小兵决定兵线和推塔节奏
- 防御塔决定风险边界
- 子弹决定技能/普攻规避

### 5.2 LSTM + 旁路 MLP

去年模型同时使用：

- LSTM 路径：处理时间序列
- 旁路 MLP：保留当前帧即时信息

二者拼接后再输出策略和值函数。这比只用 MLP 更强，因为 1v1 存在技能 CD、子弹轨迹、buff 层数、兵线节奏等时间依赖。

### 5.3 target 注意力头

去年最值得迁移的模型设计是 target 输出头：

1. 模型先对敌方英雄、己方英雄、敌方 4 小兵、敌塔等 target 实体产生 embedding。
2. LSTM 输出产生 value/query。
3. 用注意力方式得到 9 维 target logits。

这和动作协议天然匹配：

- 0：None
- 1：Enemy
- 2：Self
- 3~6：Soldier
- 7：Tower
- 8：Monster/None placeholder

今年 target 定义仍然是 9 维，因此这个设计应该重点保留。

### 5.4 多英雄输出头

去年支持 3 个英雄，`Config.MULTI_HEAD=True`，每个英雄有独立 policy/value 输出头。

今年只有 2 个英雄：

- 鲁班七号 `112`
- 狄仁杰 `133`

因此如果迁移多头，需要改成 2 个输出头，而不是 3 个。也可以先用单头，后续再做双头。建议：

1. 第一版使用单头，降低迁移风险。
2. 第二版启用双头：鲁班头、狄仁杰头。

原因：今年两个英雄技能差异大，但都是射手，单头可能也能学到基础策略；双头需要同步改 learner batch 里的 hero split 逻辑，风险更高。

---

## 6. 去年训练流程设计

去年 workflow 特点：

1. 支持 selfplay。
2. 支持自动换边。
3. 支持多英雄阵容轮换。
4. 支持训练中评估对手随机轮换 `eval_opponent_types`。
5. 记录额外 reward 子项到监控。
6. 有 debug agent 和帧保存工具，方便研究协议。

今年官方 workflow 已经重构：

- `env.reset` 返回 `env_obs` dict
- `env.step` 返回 `(env_reward, env_obs)`
- `observation` 用字符串 key：`observation["0"]` / `observation["1"]`
- sample 通过 `agent.send_sample_data(...)` 发送
- 有 `init_config` 召唤师技能注入流程

迁移时建议以今年官方 `agent_ppo/workflow/train_workflow.py` 为骨架，而不是直接使用去年 workflow。

应该迁移的是：

- 阵容轮换思想
- 多评估对手随机轮换思想
- reward 子项监控
- debug frame 保存工具（可选）

不应直接迁移：

- 去年的 `env.reset` / `env.step` 调用格式
- 去年的 observation list 索引
- 去年的 wrapper 装饰器

---

## 7. 今年与去年代码的主要差异

### 7.1 框架接口差异

| 项目 | 去年 | 今年 |
| --- | --- | --- |
| Agent 基类 | `kaiwu_agent.agent.base_agent.BaseAgent` | `kaiwudrl.interface.agent.BaseAgent` |
| wrapper | 大量使用 `predict_wrapper`、`learn_wrapper`、`attached` | 今年官方 baseline 基本不使用这些 wrapper |
| SampleData 字段 | `npdata` | `sample` |
| learn 发送样本 | `agent.learn(g_data)` | `agent.send_sample_data(g_data)` |
| env.reset | `observation, extra_info = env.reset(...)` | `env_obs = env.reset(...)` |
| env.step | `frame_no, observation, terminated, truncated, extra_info = env.step(...)` | `env_reward, env_obs = env.step(actions)` |
| observation key | `observation[i]` | `observation[str(i)]` |

结论：今年必须保留今年官方 workflow/Agent 外壳，只把去年内部设计迁移进去。

### 7.2 observation 协议差异

去年字段形态：

- `frameNo`
- `actor_state`
- `actor_state["values"]`
- `moneyCnt`
- `killCnt`
- `deadCnt`
- `totalHurtToHero`
- `isInGrass`
- `camp` 多为字符串如 `PLAYERCAMP_1`
- `actor_type` 多为字符串如 `ACTOR_HERO`
- `sub_type` 多为字符串如 `ACTOR_SUB_SOLDIER`

今年字段形态：

- `frame_no`
- 英雄字段直接平铺在 hero dict 中
- `ep/max_ep/phy_atk/...` 直接在 Hero/NPC dict 中
- `money_cnt`
- `kill_cnt`
- `dead_cnt`
- `total_hurt_to_hero`
- `is_in_grass`
- `camp` 可能为 `PLAYERCAMP_1` / `PLAYERCAMP_2` 或整数
- `actor_type/sub_type` 可能为 int 或枚举字符串，官方 baseline 中塔用 `sub_type == 21`

结论：去年 `unpack_state_dict.py` 不能直接用，需要重写为今年协议版。

### 7.3 英雄差异

去年：

- 后羿 `169`
- 李元芳 `173`
- 虞姬 `174`

今年：

- 鲁班七号 `112`
- 狄仁杰 `133`

影响：

1. `HERO_CONFIG_ID` 改为 `[112, 133]`
2. 多输出头从 3 头改 2 头，或者先单头
3. buff ID 白名单全部需要重新采集
4. mark 层数配置不能直接用去年 `16900/17300/...`
5. 召唤师技能选择需要新增 `init_config`

### 7.4 地图/单位差异

去年代码考虑：

- 河蟹
- 血包 cake
- 小兵三类型：近战、远程、炮车
- 防御塔、水晶、泉水塔
- 子弹

今年文档：

- 任务终止是防御塔被摧毁
- `cakes` 字段仍在协议里
- target 仍有 `Monster`
- `npc_states` 仍含小兵、防御塔、野怪等

差异不确定点：

1. 今年 1v1 是否仍有河蟹，需要平台帧确认。
2. 今年小兵 `config_id` 是否仍为 `6800~6805`，需要帧确认。
3. 今年 `actor_type/sub_type` 的实际类型是 int 还是 str，需要运行帧确认。

迁移方案要把这些做成兼容判断，而不是硬编码单一格式。

### 7.5 训练配置差异

去年配置支持 `eval_opponent_types` 多模型轮换；今年官方 `train_env_conf.toml` 只声明单个 `eval_opponent_type`，虽然可以自己扩展 workflow 读额外字段，但要确认 `read_usr_conf` 是否允许未知字段。

稳妥方案：

1. 第一版使用今年官方 `EnvConfManager`。
2. 第二版再扩展自己的 `EnvConfManager`，添加多个评估对手轮换。

---

## 8. 基于去年代码改造今年参赛代码的总体路线

建议不要直接复制去年 `agent_lastyear` 到 `agent_diy` 然后改到能跑。更稳的路线是：

1. 用今年 `agent_ppo` 作为框架外壳。
2. 用去年冠军代码作为内部核心设计来源。
3. 分四个阶段迁移：

| 阶段 | 目标 |
| --- | --- |
| A | 恢复 `agent_diy` 为今年可运行 PPO 外壳 |
| B | 迁移去年 `Info/ObsBuilder/RewardManager` 思想，重写今年协议版 |
| C | 迁移去年实体模块化模型和 target 注意力头 |
| D | 训练流程增强：阵容轮换、召唤师技能、多评估对手、监控、debug |

---

## 9. 详细改造方案

### 9.1 第一阶段：建立今年协议外壳

目标：`agent_diy` 重新具备今年平台可运行结构。

建议从今年 `agent_ppo` 复制以下文件作为外壳：

- `agent.py`
- `algorithm/algorithm.py`
- `feature/definition.py`
- `workflow/train_workflow.py`
- `conf/conf.py`
- `conf/monitor_builder.py`
- `conf/train_env_conf.toml`

然后把模块路径改为 `agent_diy`。

这一阶段不引入去年特征，先用今年 baseline 的 10 维特征跑通。

验收：

- `python -m compileall agent_diy`
- 模型前向 shape 正确
- 平台 `train_test.py` 可跑通

### 9.2 第二阶段：今年协议版 Info 解包层

新增：

- `agent_diy/feature/state_info.py`

实现今年协议版：

- `Info`
- `HeroInfo`
- `ActorInfo`
- `SkillInfo`
- `SlotInfo`
- `OrganInfo`
- `SoldierInfo`
- `CakeInfo`
- `BulletsInfo`
- `DeadsInfo`

关键要求：

1. 支持字段兼容访问：
   - `frame_no` / `frameNo`
   - `money_cnt` / `moneyCnt`
   - `kill_cnt` / `killCnt`
   - `is_in_grass` / `isInGrass`
2. 支持 camp 兼容：
   - `1/2`
   - `"PLAYERCAMP_1"` / `"PLAYERCAMP_2"`
3. 支持 actor type 兼容：
   - int 枚举
   - 字符串枚举
4. 缺失单位自动填 padding，而不是 assert 崩溃。

今年 `ActorInfo` 应优先读取平铺字段：

- `hp/max_hp`
- `ep/max_ep`
- `phy_atk/phy_def`
- `mgc_atk/mgc_def`
- `mov_spd/atk_spd`
- `buff_state`
- `hit_target_info`

### 9.3 第三阶段：今年版 ObsBuilder

新增：

- `agent_diy/feature/obs_builder.py`

从去年 `ObsBuilder` 迁移设计，但配置换成今年：

```python
HERO_CONFIG_ID = [112, 133]
```

初始建议特征模块：

1. 英雄：
   - 己方英雄
   - 敌方英雄
2. 小兵：
   - 己方最近 4 个
   - 敌方最近 4 个
3. 防御塔：
   - 己方防御塔
   - 敌方防御塔
4. 子弹：
   - 敌方英雄子弹最多 9 个
   - 敌方防御塔子弹 1 个
5. 野怪/河蟹：
   - 如果今年帧里有则启用，否则填 0
6. 血包：
   - 如果今年帧里有则启用，否则填 0

不建议第一版迁移英雄专属 buff 白名单。第一版可以：

- 保留通用 buff unknown 位
- 记录实际出现的 buff id
- 训练一段时间后再把高频 buff id 写入白名单

### 9.4 第四阶段：今年版 RewardManager

从去年 `reward_manager.py` 迁移思想，但字段改今年协议。

推荐第一版 reward：

```python
REWARD_WEIGHT_DICT = {
    "tower_hp_point": 10.0,
    "hp_point": 2.0,
    "money": 4e-3,
    "exp": 4e-3,
    "death": -1.0,
    "kill": 0.6,
    "last_hit": 0.5,
    "forward": 0.01,
}
```

保留：

- `TIME_SCALE_ARG = 8000`
- `REMOVE_FORWARD_AFTER = 1000`

需要注意：

1. `kill` 的正负号要单独做实验，不建议盲从去年。
2. 今年胜负是推塔，`tower_hp_point` 必须保持最高权重。
3. `forward` 必须早期关闭，否则会鼓励无意义压塔。

### 9.5 第五阶段：模型迁移

第一版模型建议采用去年 `model.py` 的“单头版本”：

- 实体模块化编码
- LSTM + 旁路 MLP
- target 注意力头
- 单 policy/value 输出头

暂不启用多头。

理由：

1. 今年只有两个英雄，单头足够先验证特征和 reward。
2. 多头需要在样本训练时按英雄拆 batch，增加调试复杂度。
3. target 注意力头比多头更关键，优先迁移。

第二版再启用双头：

- 鲁班头
- 狄仁杰头

需要改：

- `Config.MULTI_HEAD = True`
- `HERO_CONFIG_ID = [112, 133]`
- `Model` 中 `ModuleList` 长度 2
- `Algorithm.learn(list_sample_data, hero_split_nums)`
- `FrameCollector` 或 workflow 记录每条样本英雄 id

### 9.6 第六阶段：训练流程迁移

以今年 `agent_ppo/workflow/train_workflow.py` 为基础，迁移去年优点：

1. 阵容轮换：
   - `[112,112]`
   - `[112,133]`
   - `[133,112]`
   - `[133,133]`
2. `init_config` 召唤师技能：
   - 鲁班：闪现/疾跑
   - 狄仁杰：终结/净化/闪现
3. 训练中评估：
   - 初期 common_ai
   - 中期 selfplay
   - 后期指定历史模型
4. 监控：
   - `reward_sum`
   - `hp_point_weight`
   - `tower_hp_point_weight`
   - `money_weight`
   - `last_hit_weight`
   - `kill/death`

---

## 10. 今年实现建议的文件结构

建议重建 `agent_diy`：

```text
agent_diy/
├── agent.py
├── algorithm/
│   ├── __init__.py
│   └── algorithm.py
├── conf/
│   ├── __init__.py
│   ├── conf.py
│   ├── monitor_builder.py
│   └── train_env_conf.toml
├── feature/
│   ├── __init__.py
│   ├── definition.py
│   ├── state_info.py
│   ├── obs_builder.py
│   └── reward_manager.py
├── model/
│   ├── __init__.py
│   └── model.py
└── workflow/
    ├── __init__.py
    ├── lineup_heroes.py
    └── train_workflow.py
```

可选 debug 工具：

```text
agent_diy/debug/
└── frames/
```

---

## 11. 迁移优先级

强烈建议按下面顺序实现，不要一次性把去年全部代码复制进来：

1. 今年外壳跑通。
2. 今年版 `Info` 解包跑通。
3. 用 `ObsBuilder` 输出固定维度特征，先接简单 MLP 模型验证。
4. 替换为去年实体模块化模型。
5. 开启 target 注意力头。
6. 增强 reward。
7. 增加多评估对手/自对弈策略。
8. 最后才考虑双英雄多头。

---

## 12. 关键风险

### 12.1 协议字段风险

去年代码大量依赖旧字段名，直接运行会失败。例如：

- `frameNo` vs `frame_no`
- `actor_state` vs 平铺 Hero/NPC 字段
- `values["ep"]` vs `ep`
- `moneyCnt` vs `money_cnt`
- `sub_action_mask[action[0]]` vs `sub_action_mask[str(action[0])]`

解决：先做协议适配层。

### 12.2 英雄专属 buff 风险

去年 buff 白名单完全是后羿、李元芳、虞姬，不适用于鲁班和狄仁杰。

解决：

1. 第一版只保留 unknown buff 位。
2. 在平台保存调试帧。
3. 统计鲁班/狄仁杰实际 buff id。
4. 第二版再写入白名单。

### 12.3 小兵/野怪 config_id 风险

去年小兵 config id 和河蟹 config id 可能变化。

解决：

1. 以 `sub_type` 判断小兵优先。
2. `config_id` 只用于细分小兵类型。
3. unknown 类型保底归类为普通小兵或填 0。

### 12.4 模型维度风险

去年模型维度巨大，所有 `Args.DIM_*` 必须和 `ObsBuilder` 输出严格一致。

解决：

1. 所有模块写 assert。
2. 构造假帧单测输出维度。
3. 先跑模型前向，再进平台训练。

---

## 13. 推荐的最终路线

推荐今年最终方案：

1. 框架外壳：今年 `agent_ppo`
2. 特征：去年 `Info + ObsBuilder` 思路，按今年协议重写
3. 模型：去年实体模块化模型 + LSTM + target 注意力头
4. 奖励：去年 reward 结构，权重按今年推塔目标重新调
5. 训练：今年 workflow + 去年阵容轮换和多评估对手思想
6. 召唤师技能：今年新增 `init_config` 规则策略

一句话总结：

> 不要“直接移植去年代码”，而要“用今年官方代码承接平台接口，用去年冠军代码重建特征、模型和奖励核心”。

